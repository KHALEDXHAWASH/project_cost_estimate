project.cost.estimate (Main model)

Represents the overall cost estimation for a project.
Fields:

  #  name: Char (e.g. "Cost Estimate for Website Project")

  # project_id: Many2one → project.project (linked project)

  #  estimated_total_cost: Float (computed from breakdown lines)

   # breakdown_ids: One2many → project.cost.breakdown (estimate_id)

project.cost.breakdown (Child lines)

Represents individual cost items, like labor, equipment, software licenses, etc.
# Fields:
 name: Char (description)

# quantity: Float
# unit_cost: Float

# subtotal: Float (computed = quantity × unit_cost)

**UI Requirements
Add a smart button in the project.project form to view related cost estimates
add latest cost analysis in project kanban view
create kanban view for main model

Access & Security
#every project user can create cost analysis(>>draft)
#only project admin can submit cost analysis(>>submited)
#only users with specific group can reject or approve cost estimated
#after submitting all fields become read only
when state become reject or approve send an  email for user created the cost estimate